module Krikit.Version (versionString) where

-- | Display version. Kept deliberately dumb for the v0.1 scaffold;
-- once we need dynamic version from the cabal file we can switch to
-- Paths_krikit_agent_ops.version.
versionString :: String
versionString = "0.1.0.0"
